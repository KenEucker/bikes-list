<button type="button"
        class="admin-theme-toggle btn btn-link link-body-emphasis p-1 me-1 rounded border-0 d-flex align-items-center justify-content-center"
        style="min-width: 2rem; min-height: 2rem;"
        aria-label="{{ __('Toggle theme') }}"
        title="{{ __('Toggle theme') }}"
        data-label-light="{{ __('Switch to dark mode') }}"
        data-label-dark="{{ __('Switch to light mode') }}">
    <span class="admin-theme-icon" aria-hidden="true"></span>
</button>
